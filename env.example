# Moltbot + Ollama (CPU-only). Copy to .env and set values.
# Used by docker-compose; same file works for Dockge on your server.
#
# IMPORTANT: Before starting, create data directories with correct permissions:
#   mkdir -p ./data/moltbot-config ./data/moltbot-workspace
#   sudo chown -R 1000:1000 ./data/

# ---- Gateway (required for dashboard auth) ----
# Generate a secret: openssl rand -hex 32
CLAWDBOT_GATEWAY_TOKEN=

# ---- Ports ----
CLAWDBOT_GATEWAY_PORT=18789
CLAWDBOT_BRIDGE_PORT=18790

# ---- Persistence (paths on host) ----
# These directories must be owned by UID 1000 (node user in container)
CLAWDBOT_CONFIG_DIR=./data/moltbot-config
CLAWDBOT_WORKSPACE_DIR=./data/moltbot-workspace

# ---- Ollama provider ----
# Set to any value (e.g. ollama-local) to enable Ollama in Moltbot.
OLLAMA_API_KEY=ollama-local
# Gateway uses this to reach Ollama (Docker network). Override if Ollama runs elsewhere.
OLLAMA_BASE_URL=http://ollama:11434

# ---- Web search (optional) ----
# Brave Search API key for web_search tool. Get one at https://brave.com/search/api/
# (Data for Search plan). Or run: moltbot configure --section web
# BRAVE_API_KEY=

# ---- Telegram (optional) ----
# Bot token from @BotFather (Telegram). Or set channels.telegram.botToken in moltbot.json
# TELEGRAM_BOT_TOKEN=

#
# ---- Optional: production / Dockge ----
# For remote access, set GATEWAY_AUTH_TOKEN and use Tailscale/Cloudflare Tunnel.
# GATEWAY_AUTH_TOKEN=
